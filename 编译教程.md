# GitHub 编译零基础详细教程（Windows）

> 从头到尾每一步都写清楚。你只需要：**源码**（已在桌面）+ **GitHub 账号** + **git**（已装好）。
> 全程照着做，做完就能得到 ipa，TrollStore 装上即用。

---

## 第 0 步：确认三样东西

1. 源码目录：`C:\Users\Administrator\Desktop\三角洲源码\给神之眼的项目`
2. git：已装好（2.55 版本）✅
3. GitHub 账号：没有就跟着第 1 步注册

---

## 第 1 步：注册 GitHub 账号（已有可跳过）

1. 打开浏览器，访问 **https://github.com**
2. 点右上角 **Sign up**
3. 填邮箱 → 设置密码 → 填用户名（记下来，后面要用）→ 验证邮箱
4. 登录进去

---

## 第 2 步：新建私有仓库

1. 登录后，点右上角 **+** → **New repository**
2. 填写：
   - **Repository name**：随便填，比如 `delta-ios`
   - **Private** 必须勾选（防查！别选 Public）
   - 其他不用管
3. 点底部绿色 **Create repository**
4. 创建后停留在这个页面，页面上会显示一个仓库地址，类似：
   ```
   https://github.com/你的用户名/delta-ios.git
   ```
   把这个地址**复制下来**，第 5 步要用。

---

## 第 3 步：生成 Personal Access Token（PAT，就是密码）

> 从 2021 年起，GitHub 上传代码不能用账号密码，要用 Token。一次性生成。

1. 点右上角你的**头像** → 最下面 **Settings**
2. 页面左侧拉到底，点 **Developer settings**
3. 左侧点 **Personal access tokens** → **Tokens (classic)**
4. 点右上角 **Generate new token** → **Generate new token (classic)**
5. 填写：
   - **Note**：随便填，比如 `build`
   - **Expiration**：选 **No expiration**（不过期）
   - **权限勾选**：勾上最上面的 **repo**（一整行全勾）
6. 拉到最下面，点绿色 **Generate token**
7. 页面会显示一串 `ghp_` 开头的字符串，**立刻复制保存**（关掉页面就再也看不到了）

> 这串 `ghp_xxx` 就是你的"上传密码"，第 5 步 push 时要填。

---

## 第 4 步：打开 Git Bash，进入源码目录

1. 在桌面空白处**右键** → **Git Bash Here**（装了 git 才有这个选项）
   或者开始菜单搜 **Git Bash** 打开
2. 输入下面命令进入源码目录：
   ```bash
   cd "C:/Users/Administrator/Desktop/三角洲源码/给神之眼的项目"
   ```

---

## 第 5 步：删嵌套 .git + 上传（关键）

**逐条复制粘贴执行**（一条一条来，别一次全贴）：

```bash
# ① 删除嵌套的 .git 目录（不删会导致 GitHub 当子模块处理，编译缺文件）
find . -name ".git" -type d -exec rm -rf {} + 2>/dev/null

# ② 初始化仓库
git init

# ③ 添加所有文件
git add .

# ④ 提交
git commit -m "init"

# ⑤ 分支改名为 main
git branch -M main

# ⑥ 关联远程仓库（把"你的用户名"和"delta-ios"换成你自己的）
git remote add origin https://github.com/你的用户名/delta-ios.git

# ⑦ 推送上传（这一步会要你登录）
git push -u origin main
```

执行第 ⑦ 步时，会弹出/提示输入：
- **Username**：填你的 GitHub 用户名
- **Password**：粘贴第 3 步生成的 `ghp_xxx` Token（**不是你的登录密码！**）

> 输入密码时屏幕不显示任何字符，是正常的，粘贴完直接回车。

上传 115MB 大概几分钟，等它跑完（看到 `branch 'main' set up` 之类就成功了）。

---

## 第 6 步：触发编译

上传成功 = **自动开始编译**（workflow 监听了 push）。想看进度或手动触发：

1. 浏览器打开你的仓库页面
2. 点顶部 **Actions** 标签
3. 左侧看到 **编译三角洲iOS成品** 在跑（黄色圈 = 进行中）
4. 如果想手动触发：右侧 **Run workflow** → 绿色 **Run workflow**

编译大约 **5-10 分钟**，跑完变绿色 ✓。

---

## 第 7 步：下载成品

1. Actions 页面，点进**这一次**的运行记录
2. 拉到页面底部 **Artifacts** 区域
3. 点 **三角洲成品ipa** 下载
4. 解压（.zip），得到 `三角洲成品.ipa`

---

## 第 8 步：装到 iPad（启动即用）

1. 把 ipa 传到 iPad mini 6（隔空投送 / 微信文件传输 / 数据线都行）
2. 用 **TrollStore（巨魔）** 打开安装
3. 打开 App → **自动完成**：
   - 自动跑 landa 内核漏洞（已从被封的 physpuppet 换过来）
   - 自动拿权限读游戏内存
   - 自动起遍历 + 自瞄线程
4. 进游戏对局，自瞄生效

---

## 常见问题

| 问题 | 解决 |
|---|---|
| push 时报 403 / 认证失败 | Token 没勾 repo 权限，或粘贴错了，重新生成一个 |
| push 提示 rejected（仓库非空） | 仓库创建时可能勾了 README，执行 `git pull origin main --allow-unrelated-histories` 再 push |
| Actions 编译红叉 | 点进看红色报错日志，截图发我 |
| 下载的 ipa 装不上 | 确认用 TrollStore 装，不是用 Filza 直接点 |
| 自瞄没效果 | 先确认游戏进对局了，再看 `自瞄.h` 里开关是不是 true |

---

## 改参数去哪改

| 想改 | 文件 |
|---|---|
| 自瞄开关/平滑/FOV/锁头锁胸 | `ImGui绘制/图层/绘制/三角洲行动/自瞄.h` 顶部 |
| 内核漏洞 flavor | `Exploit/DOExploitManager.m` 的 `selectedKernelExploit` |
| 游戏偏移 | `子进程.h` + `HUD视图控制器.mm` |

改完代码，`git add . && git commit -m "改" && git push` 重新推，Actions 会自动重编译。
